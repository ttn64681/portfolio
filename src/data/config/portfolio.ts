/**
 * RAG corpus entry point. Implementation lives in `./rag/` (one file per section).
 * Edit section files, then run `npm run rag:sync` before deploy.
 */
export { portfolioDocuments, toDocument } from './rag';
